OPENAI_API_KEY=your_openai_api_key
JWT_SECRET=replace-with-a-long-random-string-at-least-32-chars
GITHUB_TOKEN=your_github_token

# Database — leave unset for local PGlite dev.
# On Vercel, these are injected automatically by the Neon Storage integration.
# STORAGE_DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# Optional: override local PGlite data directory (defaults to ./data/finance.pgdata)
# DATABASE_PATH=./data/finance.pgdata